mod individual;
mod population;
mod stats;

pub use self::individual::*;
pub use self::population::*;
pub use self::stats::*;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
